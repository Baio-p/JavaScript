public class Arreglos {

    public static void main(String[] args) {

	//inicializamos el arreglo con el tipo y los valores obtenidos
        int numeros[] = {12, 10, 4, 24, 5, 9};

        System.out.println(numeros[0]);
	
	// //Creamos un ciclo donde el valor iterativo incrementa hasta el tamaño del arreglo
        // for (int lugar = 0; lugar < numeros.length; lugar++) {
		
	// //imprimimos el valor iterativo consecutivamente seguido de la posicion equivalente en el arreglo
        //         System.out.println("El arreglo en la posición " + lugar + " tiene el número " + numeros[lugar]);
        // }
    } 
}